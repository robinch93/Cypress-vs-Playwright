const Filters = {
    sortByDropdown: '#products-orderby',
    display: '#products-pagesize',
    viewAs: '#products-viewmode',
    price: '.price-range-selector li a'
}

const Product = {
    title: '.product-title',
    price: '.actual-price',
    rating: '.rating',
    image: '.picture a img',
    addToCart: '.add-info div input'
}

const Page = {
    title: '.page-title'
}

module.exports = { Filters, Product }